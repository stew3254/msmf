// noinspection JSUnresolvedVariable

config.devServer = {
    /**
     * When using the dev server, fallback to index.html for unknown routes likely created by React Router.
     */
    historyApiFallback: true
};
